description: provides sample model.
queries:
  Select:
    description: select by ID and Title.
    statement: SELECT * FROM Sample;
    prepared: true
    result:
      type: Row
      fields:
      - name: ID
        type: string
        tag: 'json:"id,string"'
      - name: Title
        type: string
      - name: Content
        type: string
