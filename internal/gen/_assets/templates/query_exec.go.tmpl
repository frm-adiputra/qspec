{{define "QUERY_EXEC"}}
// {{.Name}} {{.Description}}
func {{.Name}}({{if .ParamsDeclaration}} {{.ParamsDeclaration}} interface{} {{end}}) (sql.Result, error) {
	r, err := getDB().Exec(_{{.Name}}Stmt {{if .ParamsUsage}}, {{.ParamsUsage}} {{end}})
	return r, err
}

{{if .Prepared}}{{template "PREPARED_QUERY_EXEC" .}}{{end}}
{{end}}
