{{define "QUERY_ROW"}}
// {{.Name}} {{.Description}}
func {{.Name}}({{.ParamsDeclaration}}) ({{.ReturnType}}, error) {
	{{- if and .IsUsingParamsStruct .IsUsingParamsStructValidations -}}
	err := params.Validate()
	if err != nil {
		return nil, err
	}
	{{end}}
	var v {{.ReturnType}}
	err := getDB().QueryRow(_{{.Name}}Stmt {{if .ParamsUsage}}, {{.ParamsUsage}} {{end}}).Scan({{.RowScan}})
	if err != nil {
		return {{.ReturnType}}{}, err
	}

	return v, nil
}

{{if .Prepared}}{{template "PREPARED_QUERY_ROW" .}}{{end}}
{{end}}
