{{define "PREPARED_QUERY_EXEC"}}
{{template "PREPARED_QUERY_COMMONS" .}}

// Exec executes the prepared {{.Name}} statement.
func (p *Prepared{{.Name}}) Exec({{.ParamsDeclaration}}) (sql.Result, error) {
	r, err := p.stmt.Exec({{.ParamsUsage}})
	return r, err
}
{{end}}
